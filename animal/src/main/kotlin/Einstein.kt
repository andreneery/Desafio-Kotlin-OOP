class Einstein{

    fun character(): Animal {
        val character = Animal(
            walkOnEath = true,
            canFly = false,
            swin = true,
            isAlive = false,
            layEggs = false
        )
        return character
    }
}